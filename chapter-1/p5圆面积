#include<stdio.h>
#include<math.h>

int main()
{
    const double pi=acos(-1.0 );
    double s,s1,s2,r,h;
    scanf("%lf%lf",&r,&h);
    s1=2*pi*r*h;
    s2=2*pi*r*r;
    s=s1+s2;
    printf("Area=%.3f\n",s);

}
